using System;
                    
public class Program
{
    public static void Main()
    {
        //Programa que muestre las letras de la Z (mayúscula) a la A (mayúscula, descendiendo).
        char inversa;
        
        for (inversa = 'Z'; inversa >= 'A'; inversa--)

        Console.Write("{0}",inversa);
    }
}
